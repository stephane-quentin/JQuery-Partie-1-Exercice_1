$(function() { // On utilise la méthode .click qui porte sur l'id (le bouton ici)
  $('#hide').click(function(){ // Au clic, on utilise la méthode hide(cacher) pour cacher l'id correspondant (text)
    $('#text').hide();
    });
});
